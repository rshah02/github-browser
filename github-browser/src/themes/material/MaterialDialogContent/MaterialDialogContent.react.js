import React from 'react';
import DialogContent from '@material-ui/core/DialogContent';

function MaterialDialogContent(props) {
  return (
      <DialogContent {...props}/>
  );
}

export default MaterialDialogContent;
